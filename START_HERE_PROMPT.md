# Prompt to paste into Codex

Read `AGENTS.md`, `README.md`, and all files in `docs/`.

Then build the initial Manim Community repository described there.

Requirements:
1. Prefer `uv` for setup, but support `venv` + `pip` if needed.
2. Generate the repository structure from `docs/REPO_LAYOUT.md`.
3. Create the initial files from `docs/INITIAL_FILES.md`.
4. Add one minimal working sample scene in `scenes/lesson_001_intro.py`.
5. Add a practical `.gitignore`.
6. Write clear setup instructions for Windows PowerShell and macOS/Linux.
7. Provide exact commands for preview render and final render.
8. Do not create a large lesson library yet.
9. Summarize all created files and any assumptions.
